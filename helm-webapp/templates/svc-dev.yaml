apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-dev
  labels:
    app: {{ .Values.appName }}
    environment: dev
spec:
  ports:
    - port: 8080
      protocol: TCP
      name: flask
  selector:
    app: {{ .Values.appName }}
    tier: frontend
  type: NodePort
