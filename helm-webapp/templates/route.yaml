apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.appName }}-route
  labels:
    app: {{ .Values.appName }}
spec:
  to:
    kind: Service
    name: {{ .Values.appName }}
    weight: 100
  port:
    targetPort: flask
  wildcardPolicy: None
